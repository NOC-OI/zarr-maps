name: Publish to npm

on:
  push:
    tags:
      - 'v*' # v1.0.0, v1.2.3 etc.

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org/
          package-manager-cache: false

      - name: Update npm for trusted publishing
        run: npm install --global npm@latest

      - name: Install dependencies
        run: npm ci --include=dev

      # ------------------------------------------------------------
      # zarr-maps-colormap
      # ------------------------------------------------------------

      - name: Build zarr-maps-colormap
        run: npm run build:colormap

      - name: Stage zarr-maps-colormap
        run: |
          WORKSPACE="zarr-maps-colormap"

          PACKAGE_JSON="packages/$WORKSPACE/package.json"
          NAME=$(jq -r '.name' "$PACKAGE_JSON")
          VERSION=$(jq -r '.version' "$PACKAGE_JSON")

          echo "Checking $NAME@$VERSION"

          if npm view "$NAME@$VERSION" version > /dev/null 2>&1; then
            echo "$NAME@$VERSION already exists on npm. Skipping."
          else
            echo "Staging $NAME@$VERSION"
            npm stage publish --workspace="$WORKSPACE" --access public
          fi

      # ------------------------------------------------------------
      # zarr-maps-tiling
      # ------------------------------------------------------------

      - name: Build zarr-maps-tiling
        run: npm run build:tiling

      - name: Stage zarr-maps-tiling
        run: |
          WORKSPACE="zarr-maps-tiling"

          PACKAGE_JSON="packages/$WORKSPACE/package.json"
          NAME=$(jq -r '.name' "$PACKAGE_JSON")
          VERSION=$(jq -r '.version' "$PACKAGE_JSON")

          echo "Checking $NAME@$VERSION"

          if npm view "$NAME@$VERSION" version > /dev/null 2>&1; then
            echo "$NAME@$VERSION already exists on npm. Skipping."
          else
            echo "Staging $NAME@$VERSION"
            npm stage publish --workspace="$WORKSPACE" --access public
          fi

      # ------------------------------------------------------------
      # zarr-maps-explorer
      # ------------------------------------------------------------

      - name: Build zarr-maps-explorer
        run: npm run build:explorer

      - name: Stage zarr-maps-explorer
        run: |
          WORKSPACE="zarr-maps-explorer"

          PACKAGE_JSON="packages/$WORKSPACE/package.json"
          NAME=$(jq -r '.name' "$PACKAGE_JSON")
          VERSION=$(jq -r '.version' "$PACKAGE_JSON")

          echo "Checking $NAME@$VERSION"

          if npm view "$NAME@$VERSION" version > /dev/null 2>&1; then
            echo "$NAME@$VERSION already exists on npm. Skipping."
          else
            echo "Staging $NAME@$VERSION"
            npm stage publish --workspace="$WORKSPACE" --access public
          fi

      # ------------------------------------------------------------
      # zarr-maps-leaflet
      # ------------------------------------------------------------

      - name: Build zarr-maps-leaflet
        run: npm run build:leaflet

      - name: Stage zarr-maps-leaflet
        run: |
          WORKSPACE="zarr-maps-leaflet"

          PACKAGE_JSON="packages/$WORKSPACE/package.json"
          NAME=$(jq -r '.name' "$PACKAGE_JSON")
          VERSION=$(jq -r '.version' "$PACKAGE_JSON")

          echo "Checking $NAME@$VERSION"

          if npm view "$NAME@$VERSION" version > /dev/null 2>&1; then
            echo "$NAME@$VERSION already exists on npm. Skipping."
          else
            echo "Staging $NAME@$VERSION"
            npm stage publish --workspace="$WORKSPACE" --access public
          fi

      # ------------------------------------------------------------
      # zarr-maps-ol
      # ------------------------------------------------------------

      - name: Build zarr-maps-ol
        run: npm run build:ol

      - name: Stage zarr-maps-ol
        run: |
          WORKSPACE="zarr-maps-ol"

          PACKAGE_JSON="packages/$WORKSPACE/package.json"
          NAME=$(jq -r '.name' "$PACKAGE_JSON")
          VERSION=$(jq -r '.version' "$PACKAGE_JSON")

          echo "Checking $NAME@$VERSION"

          if npm view "$NAME@$VERSION" version > /dev/null 2>&1; then
            echo "$NAME@$VERSION already exists on npm. Skipping."
          else
            echo "Staging $NAME@$VERSION"
            npm stage publish --workspace="$WORKSPACE" --access public
          fi
